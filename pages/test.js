import { Card, Text } from '@/core'

export default function Test() {
  return (
    <Card>
      <Text color="dark" dangerouslySetInnerHTML={{ __html: text }} />
    </Card>
  )
}

const text = `<div>\n<h1 id=\"getting-started\">Getting Started</h1>\n<h2 id=\"authentication\">Authentication</h2>\n<p>Your API token is used to authenticate you with our API and can be provided either as an HTTP Basic Auth username, a bearer token, or alternatively as a token URL parameter.</p>\n<pre><code class=\"lang sql\">\n# <span class=\"hljs-keyword\">With</span> token query <span class=\"hljs-keyword\">parameter</span>\n$ curl ipinfo.io?token<span class=\"hljs-operator\">=</span>020e1f69cd6d0f\n</code></pre>\n<p>It's also possible to use the API without authentication in a more limited capacity.</p>\n<h2 id=\"json-response\">JSON Response</h2>\n<p>We try to automatically detect when someone wants to call our API versus view our website, and then we send back the appropriate JSON response rather than HTML. We do this based on the user agent for known popular programming languages, tools, and frameworks. However, there are a couple of other ways to force a JSON response when it doesn't happen automatically. One is to add <code>/json</code> to the URL, and the other is to set an <code>Accept</code> header to <code>application/json</code>:</p>\n<pre><code class=\"lang sql\"># Ensure we really <span class=\"hljs-keyword\">get</span> JSON, <span class=\"hljs-keyword\">and</span> <span class=\"hljs-keyword\">not</span> the IPinfo homepage HTML\n$ curl ipinfo.io<span class=\"hljs-operator\">/</span>json\n$ curl <span class=\"hljs-operator\">-</span>H \"Accept: application/json\" ipinfo.io\n</code></pre>\n<h2 id=\"ip-address-parameter\">IP Address Parameter</h2>\n<p>The API supports passing in a single IPv4 or IPv6 IP address. Alternatively, if you do not pass in any IP address, we'll return details for the calling address. This allows you to look up your own (or a visitor to your site) IP address details without knowing the IP address in advance.</p>\n<pre><code class=\"lang sql\"># <span class=\"hljs-keyword\">Get</span> details <span class=\"hljs-keyword\">for</span> <span class=\"hljs-number\">8.8</span><span class=\"hljs-number\">.8</span><span class=\"hljs-number\">.8</span>\n$ curl ipinfo.io<span class=\"hljs-operator\">/</span><span class=\"hljs-number\">8.8</span><span class=\"hljs-number\">.8</span><span class=\"hljs-number\">.8</span>?token<span class=\"hljs-operator\">=</span>020e1f69cd6d0f\n\n# <span class=\"hljs-keyword\">Get</span> details <span class=\"hljs-keyword\">for</span> <span class=\"hljs-number\">2001</span>:<span class=\"hljs-number\">4860</span>:<span class=\"hljs-number\">4860</span>::<span class=\"hljs-number\">8888</span>\n$ curl ipinfo.io<span class=\"hljs-operator\">/</span><span class=\"hljs-number\">2001</span>:<span class=\"hljs-number\">4860</span>:<span class=\"hljs-number\">4860</span>::<span class=\"hljs-number\">8888</span>?token<span class=\"hljs-operator\">=</span>020e1f69cd6d0f\n\n# <span class=\"hljs-keyword\">Get</span> details <span class=\"hljs-keyword\">for</span> your own IP address, which<span class=\"hljs-string\">'ll be included in the response\n$ curl ipinfo.io?token=020e1f69cd6d0f\n</span></code></pre>\n<h2 id=\"https-ssl\">HTTPS/ SSL</h2>\n<p>Our API is available over a secure HTTPS connection for all users. Simply add <code>https://</code> to the request URLs to make the requests secure.</p>\n<pre><code class=\"lang sql\"># <span class=\"hljs-keyword\">Get</span> details <span class=\"hljs-keyword\">for</span> your own IP address <span class=\"hljs-keyword\">over</span> HTTPS\n$ curl https:<span class=\"hljs-operator\">/</span><span class=\"hljs-operator\">/</span>ipinfo.io?token<span class=\"hljs-operator\">=</span>020e1f69cd6d0f\n</code></pre>\n<h2 id=\"rate-limits\">Rate Limits</h2>\n<p>Free usage of our API is limited to 50,000 API requests per month. If you exceed that limit, we'll return a 429 HTTP status code to you.</p>\n<p><a href=\"/pricing\">Paid plans</a> come with higher monthly limits, and configurable alerts.</p>\n</div>\n`
